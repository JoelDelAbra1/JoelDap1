void menu(); //Desplegar menu de opciones
void menuIEPS(); // Menu de gasolinas

float isr(float sueldo, float neto);
float siniva(float precio);
float deposito(float amount);
float coniva(float antes, float ya_con_iva);
float ieps(float litros);
float ISAN(float carro_precio, float carro_neto);


char nombre[20]; // Guargar el nombre del usuario
float cantidad; //Litros en ieps